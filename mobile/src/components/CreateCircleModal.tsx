import React, { useState } from 'react';
import { ActivityIndicator, Modal, StyleSheet, Switch, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useAuth } from '../context/AuthContext';import { useAppTheme } from '../theme';
export default function CreateCircleModal({visible,onClose,onCreated}:{visible:boolean;onClose:()=>void;onCreated:(slug:string)=>void}){const{api}=useAuth();const{theme}=useAppTheme();const[name,setName]=useState('');const[description,setDescription]=useState('');const[privateCircle,setPrivateCircle]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const create=async()=>{if(!name.trim())return setError('Escribe un nombre para el Círculo.');setLoading(true);setError('');try{const res=await api.post('/circles',{name:name.trim(),description:description.trim(),city:'',type:'GENERAL',visibility:privateCircle?'PRIVATE':'PUBLIC'});setName('');setDescription('');setPrivateCircle(false);onClose();onCreated(res.data.slug);}catch(requestError:any){setError(requestError.response?.data?.message||'No se pudo crear el Círculo.');}finally{setLoading(false);}};return <Modal visible={visible} animationType="slide" onRequestClose={onClose}><View style={[styles.container,{backgroundColor:theme.background}]}><View style={styles.header}><TouchableOpacity onPress={onClose}><Ionicons name="close" size={25} color={theme.textPrimary}/></TouchableOpacity><Text style={{color:theme.textPrimary,fontWeight:'900',fontSize:17}}>Crear Círculo</Text><TouchableOpacity disabled={loading} onPress={()=>void create()}>{loading?<ActivityIndicator color={theme.accent}/>:<Text style={{color:theme.accent,fontWeight:'900'}}>Crear</Text>}</TouchableOpacity></View>{error?<Text style={styles.error}>{error}</Text>:null}<Text style={[styles.label,{color:theme.textSecondary}]}>Nombre</Text><TextInput value={name} onChangeText={setName} placeholder="Nombre del Círculo" placeholderTextColor={theme.textMuted} style={[styles.input,{color:theme.textPrimary,backgroundColor:theme.surface,borderColor:theme.border}]}/><Text style={[styles.label,{color:theme.textSecondary}]}>Descripción</Text><TextInput value={description} onChangeText={setDescription} multiline placeholder="¿De qué trata este círculo?" placeholderTextColor={theme.textMuted} style={[styles.input,styles.description,{color:theme.textPrimary,backgroundColor:theme.surface,borderColor:theme.border}]}/><View style={[styles.privacy,{backgroundColor:theme.surface,borderColor:theme.border}]}><View style={{flex:1}}><Text style={{color:theme.textPrimary,fontWeight:'800'}}>Círculo privado</Text><Text style={{color:theme.textMuted,fontSize:11}}>Solo integrantes autorizados acceden al contenido</Text></View><Switch value={privateCircle} onValueChange={setPrivateCircle} trackColor={{false:theme.border,true:theme.primary}}/></View></View></Modal>}
const styles=StyleSheet.create({container:{flex:1,padding:18},header:{height:58,flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginBottom:18},label:{fontSize:12,fontWeight:'800',marginTop:12,marginBottom:6},input:{height:50,borderWidth:1,borderRadius:14,paddingHorizontal:14},description:{height:120,paddingTop:13,textAlignVertical:'top'},privacy:{marginTop:18,padding:15,borderRadius:15,borderWidth:1,flexDirection:'row',alignItems:'center'},error:{color:'#ef4444',textAlign:'center'}});
